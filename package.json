{
  "name": "nft-marketplace",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "initialize": "yarn && node init.js && lerna clean && yarn",
    "dev": "lerna run dev",
    "dev:client": "lerna run dev  --scope=frontend",
    "dev:server": "lerna run dev --scope=backend",
    "lint": "lerna run lint",
    "format": "lerna run format",
    "preview": "lerna run preview",
    "generate:component": "node generateComponent.js"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^8.1.9",
    "ora": "^8.2.0"
  }
}
